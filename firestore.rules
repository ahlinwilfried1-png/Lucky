rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write requests to all collections to ensure 100% fluid client-side operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
